apiVersion: v1
kind: Secret
metadata:
  name: secure-rag-secrets
type: Opaque
stringData:
  JWT_SECRET_KEY: replace-with-strong-secret
  OPENAI_API_KEY: ""
  LLM_PROVIDER: stub
  EMBEDDING_PROVIDER: local
  VECTOR_BACKEND: faiss
  FAISS_INDEX_PATH: /app/runtime/faiss_index
